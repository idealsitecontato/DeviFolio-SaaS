VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_your_key
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
# Random, server-only value with at least 32 characters. Do not expose it as VITE_*.
GITHUB_OAUTH_STATE_SECRET=
# Register this exact URL as the single "Authorization callback URL" in the
# GitHub OAuth App used by this environment. Production and localhost must use
# separate OAuth Apps (and therefore separate client ID/secret pairs).
GITHUB_CALLBACK_URL=https://devi-folio-saa-s.vercel.app/api/auth/github/callback
SUPABASE_SERVICE_ROLE_KEY=
